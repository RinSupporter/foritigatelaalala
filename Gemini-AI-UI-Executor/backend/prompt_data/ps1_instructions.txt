*   Sử dụng `try { ... } catch { ... }` để xử lý lỗi.
*   In thông báo kết quả hoặc lỗi ra `stdout` (Write-Host) hoặc `stderr` (Write-Error).
*   Đảm bảo cú pháp PowerShell hợp lệ.
*   Chú ý và xem xét xem loại file đó khi chạy có hỗ trợ tiếng việt không (PowerShell hỗ trợ tốt UTF-8 và tiếng Việt có dấu).
*   LUÔN LUÔN có cơ chế thông báo kết quả của code (ví dụ: if ... else ..., try-catch).